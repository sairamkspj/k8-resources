apiVersion : v1
kind : Pod
metadata :
  name : nginx
spec :
  containers :
  - name : nginx
    image :  nginx:stable-perl
    resources:
      requests:
        cpu: "500m"
        memory: "256Mi"

    